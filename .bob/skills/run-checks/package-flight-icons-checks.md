# Flight icons package checks

Run all of the checks listed below and ensure all pass as expected. Capture any failures and report them back to the user.

## Linting

1. Under `packages/flight-icons` run `pnpm lint`

## Typechecks

1. Under `packages/flight-icons` run `pnpm typecheck`
