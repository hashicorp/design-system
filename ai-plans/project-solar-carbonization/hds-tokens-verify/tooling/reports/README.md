# Reports

Generated by `../verify-tokens.mjs`, organized into a per-config subfolder
(controlled by `reportSubdir`; the pilot writes to `hds/`). Files are overwritten
on each run:

- `token-usage-audit.md` — human-readable summary + invalid-token table.
- `token-usage-audit.json` — machine-readable, with full location lists.
