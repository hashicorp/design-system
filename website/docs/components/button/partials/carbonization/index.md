## Secondary muted color

The `@color` argument of the Button supports a value `secondary-muted`. This variant is visually identical to the `secondary` color for HDS themes, but differs for Carbon themes. Use the `secondary-muted` color to have a secondary button on Carbon themes which is less visually dominant.

[[code-snippets/button-secondary-muted]]