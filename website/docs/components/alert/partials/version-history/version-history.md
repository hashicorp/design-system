## 6.3.0

Deprecated the `Generic` yielded block which renders below actions and added `GenericContent` and `GenericFooter` yielded blocks for including custom content either above or below `Alert` actions.


## 6.0.0

converted component and subcomponents to gts format.


## 4.15.0

Removed default color applied to the `hds-alert__text` container (text color is applied via `@color` argument)

## 4.10.0

Added `@tag` argument to `Alert::Title`
