/* eslint-disable no-unused-vars */
this.hdsTheming.setTheme({
  theme: 'dark',
  onSetTheme: ({ currentTheme, currentMode }) => {
    // do something based on the `currentTheme` / `currentMode` values
  },
});
