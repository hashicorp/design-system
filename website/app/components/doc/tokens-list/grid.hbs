{{#if @categoryTokens}}
  <ul class="doc-tokens-list">
    {{#each @categoryTokens as |token|}}
      <Doc::TokensList::Item @token={{token}} />
    {{/each}}
  </ul>
{{else}}
  <p class="doc-text-body-small">No tokens found in this category</p>
{{/if}}
