<ul class="doc-vars-list" role="list">
  {{#each @items as |item|}}
    <li class="doc-vars-list__item">
      <code>{{item}}</code>
      <CopyButton @text="{{item}}" @isFullWidth={{true}}>Copy</CopyButton>
    </li>
  {{/each}}
</ul>