<section
  class={{this.classNames}}
  role="tabpanel"
  aria-labelledby={{this.tabId}}
  id={{this.panelId}}
  {{! hidden TODO: set by arg }}
  ...attributes
  {{did-insert this.didInsertNode}}
>
  {{yield}}
</section>