<nav class="hds-pagination-nav" aria-label="Pagination navigation" ...attributes>
  <Hds::Pagination::Nav::NavButton
    @direction="prev"
    @type={{this.type}}
    @onClick={{this.onPageChange}}
    @disabled={{this.isDisabledPrev}}
  />

  {{#if (eq this.type "numbered")}}
    <ul class="hds-pagination__page-list">
      {{#each this.pages as |page|}}
        <Hds::Pagination::Nav::PageNumber
          @page={{page}}
          @onClick={{this.onPageChange}}
          @isSelected={{if (eq page @currentPage) true false}}
        />
      {{/each}}
    </ul>
  {{/if}}

  <Hds::Pagination::Nav::NavButton
    @direction="next"
    @type={{this.type}}
    @onClick={{this.onPageChange}}
    @disabled={{this.isDisabledNext}}
  />
</nav>