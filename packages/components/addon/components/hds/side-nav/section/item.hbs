<li class="hds-side-nav__list-item">
  <Hds::Interactive
    @href={{@href}}
    @route={{@route}}
    @models={{hds-link-to-models @model @models}}
    @query={{hds-link-to-query @query}}
    @icon={{@icon}}
    @isRouteExternal={{@isRouteExternal}}
    @current-when={{@current-when}}
    class="hds-side-nav__link"
    ...attributes
  >
    {{#if @icon}}
      <FlightIcon class="hds-side-nav__list-icon" @name={{@icon}} @stretched={{true}} />
    {{/if}}

    <span class="hds-typography-body-200 hds-font-weight-medium">
      {{@label}}
    </span>

    {{#if @count}}
      <Hds::BadgeCount @text={{@count}} @type="inverted" @size="small" />
    {{/if}}

    {{#if @badge}}
      <Hds::Badge @text={{@badge}} @type="inverted" @size="small" />
    {{/if}}
  </Hds::Interactive>
</li>