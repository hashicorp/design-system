<nav class={{this.classNames}} ...attributes>
  {{#if @header}}
    <div class="hds-side-nav__section-header hds-typography-body-100 hds-font-weight-semibold">
      {{@header}}
    </div>
  {{/if}}

  <ul class="hds-side-nav__list">
    {{yield (hash Item=(component "hds/side-nav/section/item"))}}
  </ul>
</nav>