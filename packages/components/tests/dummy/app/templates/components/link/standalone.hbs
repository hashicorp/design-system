{{page-title "Link::Standalone Component"}}

<h2 class="dummy-h2">
  Link::Standalone
</h2>

<section>
  <h3 class="dummy-h3">
    Overview
  </h3>

  <p class="dummy-paragraph">The Link::Standalone component handles the generation of an HTML anchor element
    <code class="dummy-code">
      &lt;a&gt;</code>
    that points to an external URL.
  </p>
  <p class="dummy-paragraph">
    What do we mean by the name
    <strong>Link "Standalone"</strong>? It's a link that's used in isolation, meaning it's not inline with adjacent
    text.
    <em>Note: Solutions for Link (Inline) are coming soon.</em>
  </p>

  <h4 class="dummy-h4">Example of Link (Standalone):</h4>
  <div class="dummy-example">
    <p class="dummy-paragraph">Here's a completely separate paragraph of text.</p>
    <br />
    <Hds::Link::Standalone
      @text="Hey, this link is a Link (Standalone)"
      href="/"
      @icon="external-link"
      @iconPosition="trailing"
    />
  </div>

  <h4 class="dummy-h4">Example of Link (Inline):</h4>
  <div class="dummy-example">
    <p class="dummy-paragraph">Here's a
      <a href="#">Link (Inline)</a>, coming soon to HDS.
      <a href="#">And another Link (Inline)</a></p>
  </div>
</section>

<section>
  <h3 class="dummy-h3">
    Component API
  </h3>
  <p class="dummy-paragraph">
    Here is the API for the component:
  </p>
  <dl class="dummy-component-props">
    <dt>
      size
      <code>
        enum
      </code>
    </dt>
    <dd>
      <p>
        Acceptable values:
      </p>
      <ol>
        <li>
          small
        </li>
        <li class="default">
          medium
        </li>
        <li>
          large
        </li>
      </ol>
    </dd>
    <dt>color <code>enum</code></dt>
    <dd>
      <p>Acceptable values:</p>
      <ol>
        <li class="default">primary</li>
        <li>secondary</li>
      </ol>
    </dd>
    <dt>
      text
      <code>
        string
      </code>
    </dt>
    <dd>
      <p>
        The text of the link (standalone).
      </p>
      <p>
        <em>
          If no text value is defined an error will be thrown.
        </em>
      </p>
    </dd>
    <dt>
      icon
      <code>
        string
      </code>
    </dt>
    <dd>
      <p>
        Use this parameter to show an icon. Acceptable value: any Flight icon name.
      </p>
      <p><strong>🚨 IMPORTANT:</strong>
        the
        <code class="dummy-code">icon</code>
        is required to make the component accessible.</p>
    </dd>
    <dt>
      iconPosition
      <code>
        enum
      </code>
    </dt>
    <dd>
      <p>
        Positions the icon before or after the text. Acceptable values:
      </p>
      <ol>
        <li class="default">
          leading
        </li>
        <li>
          trailing
        </li>
      </ol>
    </dd>
    <dt>href</dt>
    <dd>
      <p>This is the URL that is passed down ("splatted") as arguments to the
        <code>&lt;a&gt;</code>
        element (see URL handling for details).</p>
    </dd>
    <dt>“splattributes”</dt>
    <dd>
      <p><code class="dummy-code">...attributes</code> spreading is supported on this component.</p>
    </dd>
  </dl>
</section>

<section>
  <h3 class="dummy-h3">Usage</h3>

  <h4 class="dummy-h4">Basic use</h4>
  <p class="dummy-paragraph">The most basic invocation requires both
    <code class="dummy-code">icon</code>
    and
    <code class="dummy-code">text</code>
    to be passed and an
    <code class="dummy-code">href</code>
    value (for details on how URLs are handled see below).</p>
  {{! prettier-ignore-start }}
  <CodeBlock
    @language="markup"
    @code='
      <Hds::Link::Standalone @icon="film" @text="Watch tutorial video" href="..." />
    '
  />
  {{! prettier-ignore-end }}
  <p class="dummy-paragraph">Renders to:</p>
  <Hds::Link::Standalone @icon="film" @text="Watch tutorial video" href="#" />

  <h4 class="dummy-h4">Icon position</h4>
  <p class="dummy-paragraph">By default the icon is placed before the text. If you would like to position it after the
    text, define
    <code class="dummy-code">@iconPosition</code>:
  </p>
  {{! prettier-ignore-start }}
  <CodeBlock
    @language="markup"
    @code='
      <Hds::Link::Standalone @icon="film" @iconPosition="trailing" @text="Watch tutorial video" href="..." />
    '
  />
  {{! prettier-ignore-end }}
  <p class="dummy-paragraph">Renders to:</p>
  <Hds::Link::Standalone @icon="film" @iconPosition="trailing" @text="Watch tutorial video" href="#" />

  <h4 class="dummy-h4">Color</h4>
  <p class="dummy-paragraph">
    There are two available colors for a button:
    <code class="dummy-code">primary</code>
    and
    <code class="dummy-code">secondary</code>. The default is
    <code class="dummy-code">primary</code>. To use a different color, declare another value for
    <code class="dummy-code">@color</code>:
  </p>
  {{! prettier-ignore-start }}
  <CodeBlock
    @language="markup"
    @code='
      <Hds::Link::Standalone @color="primary" @icon="collections" @text="Read tutorial" href="..." />
    '
  />
  {{! prettier-ignore-end }}
  <p class="dummy-paragraph">Renders to:</p>
  <Hds::Link::Standalone @icon="collections" @text="Read tutorial" @color="primary" href="#" />
  <br />
  <Hds::Link::Standalone @icon="collections" @text="Read tutorial" @color="secondary" href="#" />

  <h4 class="dummy-h4">Size</h4>
  <p class="dummy-paragraph">
    There are three sizes available:
    <code class="dummy-code">small</code>,
    <code class="dummy-code">medium</code>
    and
    <code class="dummy-code">large</code>. The default is
    <code class="dummy-code">medium</code>. To use a different size, declare a value for
    <code class="dummy-code">@size</code>:
  </p>
  {{! prettier-ignore-start }}
  <CodeBlock
    @language="markup"
    @code='
      <Hds::Link::Standalone @icon="collections" @text="Read tutorial" @size="small" href="..." />
    '
  />
  {{! prettier-ignore-end }}
  <p class="dummy-paragraph">Renders to:</p>
  <Hds::Link::Standalone @icon="collections" @text="Read tutorial" @size="small" href="#" />
  <br />
  <Hds::Link::Standalone @icon="collections" @text="Read tutorial" @size="medium" href="#" />
  <br />
  <Hds::Link::Standalone @icon="collections" @text="Read tutorial" @size="large" href="#" />

  <h4 class="dummy-h4">URL handling</h4>
  <p class="dummy-paragraph">
    For the component to work as expected, a
    <code class="dummy-code">href</code>
    value needs to be passed as attribute. The component automatically adds
    <code class="dummy-code">target="_blank" rel="noopener noreferrer"</code>
    to the
    <code class="dummy-code">&lt;a&gt;</code>
    element (but their values can be overwritten passing those attributes to the component).
  </p>
  {{! prettier-ignore-start }}
  <CodeBlock
    @language="markup"
    @code='
      <Hds::Link::Standalone @icon="terraform" @text="Request a demo" href="https://www.hashicorp.com/request-demo/terraform" />
    '
  />
  {{! prettier-ignore-end }}
  <p class="dummy-paragraph">Renders to:</p>
  <Hds::Link::Standalone
    @icon="terraform"
    @text="Request a demo"
    href="https://www.hashicorp.com/request-demo/terraform"
  />
</section>

<section>
  <h3 class="dummy-h3">Design guidelines</h3>
  <div class="dummy-design-guidelines">
    <p class="dummy-paragraph">
      <a
        href="https://www.figma.com/file/noyY6dUMDYjmySpHcMjhkN/HDS-Product---Components-%5BWIP%5D?node-id=2522%3A8014"
        target="_blank"
        rel="noopener noreferrer"
      >
        Figma UI Kit
      </a>
    </p>
    <img class="dummy-figma-docs" src="/assets/images/design-guidelines-link-standalone.png" alt="" role="none" />
  </div>
</section>

<section data-test-percy>
  <h3 class="dummy-h3">
    Showcase
  </h3>
  <h4 class="dummy-h4">
    Content
  </h4>
  <div class="dummy-link-base-sample">
    <Hds::Link::Standalone @icon="plus" @text="Text & leading icon" href="../components/link" />
    <Hds::Link::Standalone
      @icon="arrow-right"
      @text="Text & trailing icon"
      href="../components/link"
      @iconPosition="trailing"
    />
    <div class="dummy-link-max-width-container">
      <Hds::Link::Standalone
        @icon="plus"
        @text="Very long text that might wrap for multiple lines"
        href="../components/link"
      />
    </div>
  </div>
  <h4 class="dummy-h4">
    Sizes
  </h4>
  <div class="dummy-link-base-sample">
    {{#each @model.SIZES as |size|}}
      <Hds::Link::Standalone @icon="plus" @text={{capitalize size}} @size={{size}} @route="components.link-to" />
    {{/each}}
  </div>
  <h4 class="dummy-h4">
    States (in each size)
  </h4>
  <div class="dummy-link-states-grid">
    {{#each @model.COLORS as |color|}}
      <h5 class="dummy-h5 dummy-link-states-grid__title">{{capitalize color}}</h5>
      {{#each @model.SIZES as |size|}}
        {{#each @model.STATES as |state|}}
          <div>
            <span class="dummy-text-small">
              {{capitalize size}}/{{capitalize state}}:
            </span>
            <br />
            <Hds::Link::Standalone
              @icon="plus"
              @text={{capitalize state}}
              @size={{size}}
              @color={{color}}
              class="is-{{state}}"
              href="../components/link"
            />
          </div>
        {{/each}}
      {{/each}}
    {{/each}}
  </div>
</section>