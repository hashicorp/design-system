{{!
  Copyright (c) HashiCorp, Inc.
  SPDX-License-Identifier: MPL-2.0
}}

{{page-title "Badge component"}}

<Shw::Text::H1>Badge</Shw::Text::H1>

<section data-test-percy>

  <h4 class="dummy-h4">Content</h4>

  <Shw::Sample::Flex as |SF|>
    <SF.Item>
      <Hds::Badge @text="Only text" />
    </SF.Item>
    <SF.Item>
      <Hds::Badge @icon="activity" @text="Text + icon" />
    </SF.Item>
    <SF.Item>
      <Hds::Badge @icon="activity" @text="Only icon" @isIconOnly={{true}} />
    </SF.Item>
    <SF.Item {{style width="200px"}}>
      <Hds::Badge @icon="activity" @text="This is a very long text that should go on two lines" />
    </SF.Item>
  </Shw::Sample::Flex>
  <Shw::Sample::Flex as |SF|>
    <SF.Item>
      <p>This is a paragraph:
        <Hds::Badge @icon="activity" @text="Lorem ipsum" /></p>
    </SF.Item>
  </Shw::Sample::Flex>

  <h4 class="dummy-h4">Size</h4>

  <Shw::Sample::Flex as |SF|>
    {{#each @model.BADGE_SIZES as |size|}}
      <SF.Item>
        <Hds::Badge @icon="activity" @text={{capitalize size}} @size={{size}} />
      </SF.Item>
    {{/each}}
  </Shw::Sample::Flex>

  <h4 class="dummy-h4">Type</h4>

  <Shw::Sample::Flex as |SF|>
    {{#each @model.BADGE_TYPES as |type|}}
      <SF.Item>
        <Hds::Badge @icon="activity" @text={{capitalize type}} @type={{type}} />
      </SF.Item>
    {{/each}}
  </Shw::Sample::Flex>

  <h4 class="dummy-h4">Color:</h4>

  {{#each @model.BADGE_COLORS as |color|}}
    <Shw::Sample::Grid @title={{capitalize color}} @columns={{3}} as |SG|>
      {{#each @model.BADGE_SIZES as |size|}}
        {{#each @model.BADGE_TYPES as |type|}}
          <SG.Item class="shw-component-badge-sample-color--{{color}}">
            <Hds::Badge @icon="activity" @text="Lorem ipsum" @size={{size}} @type={{type}} @color={{color}} />
            <Hds::Badge @text="Lorem ipsum" @size={{size}} @type={{type}} @color={{color}} />
            <Hds::Badge
              @icon="activity"
              @text="Lorem Ipsum"
              @size={{size}}
              @type={{type}}
              @color={{color}}
              @isIconOnly={{true}}
            />
          </SG.Item>
        {{/each}}
      {{/each}}
    </Shw::Sample::Grid>
  {{/each}}

  <h4 class="dummy-h4">Containers</h4>

  <Shw::Sample::Grid @columns={{3}} as |SG|>
    {{#let (array "block" "flex" "grid") as |displays|}}
      {{#each displays as |display|}}
        <SG.Item>
          <Shw::Sample::Label>Parent with <code>display: {{display}}</code></Shw::Sample::Label>
          <div class="shw-component-badge-group" {{style display=display}}>
            <Hds::Badge @text="Only text" /><Hds::Badge @icon="activity" @text="Text + icon" /><Hds::Badge
              @icon="activity"
              @text="Only icon"
              @isIconOnly={{true}}
            />
          </div>
        </SG.Item>
      {{/each}}
    {{/let}}
  </Shw::Sample::Grid>

</section>