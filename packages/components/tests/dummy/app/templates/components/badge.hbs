{{page-title "Badge"}}

<h1>Badge</h1>
{{#each @model.COLORS as |color|}}
<h2>{{color}}</h2>
    <ul class="dummy-ul">
        {{#each @model.SIZES as |size|}}
            {{#each @model.TYPES as |type|}}
                <li>
                    <Hds::Badge
                        @text={{concat type " / " color " / " size }}
                        @size={{size}}
                        @type={{type}}
                        @color={{color}}
                    />
                </li>
            {{/each}}
        {{/each}}
    </ul>
{{/each}}