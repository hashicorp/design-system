{{page-title "Badge"}}

<h2 class="dummy-h2">Badge</h2>

<h3 class="dummy-h3">Content</h3>
<div class="dummy-badge-base-sample">
    <Hds::Badge @text="only text" />
    <Hds::Badge @icon="activity" @text="text + icon" />
    <Hds::Badge @icon="activity" @srOnlyText="only icon" />
    <div class="dummy-badge-max-width-container">
        <Hds::Badge @icon="activity" @text="This is a very long text that should go on two lines" />
    </div>
</div>
<div class="dummy-badge-base-sample">
    <p>This is a paragraph: <Hds::Badge @icon="activity" @text="Lorem ipsum" /></p>
</div>

<h3 class="dummy-h3">Size</h3>
<div class="dummy-badge-base-sample">
    {{#each @model.SIZES as |size|}}
        <Hds::Badge @icon="activity" @text={{size}} @size={{size}} />
    {{/each}}
</div>

<h3 class="dummy-h3">Type</h3>
<div class="dummy-badge-base-sample">
    {{#each @model.TYPES as |type|}}
        <Hds::Badge @icon="activity" @text={{type}} @type={{type}} />
    {{/each}}
</div>

<h3 class="dummy-h3">Color:</h3>
{{#each @model.COLORS as |color|}}
    <p class="dummy-h6">{{color}}</p>
    <div class="dummy-badge-color-grid">
        {{#each @model.SIZES as |size|}}
            {{#each @model.TYPES as |type|}}
                <div class="dummy-badge-base-sample dummy-badge-base-sample--type-{{type}} dummy-badge-base-sample--color-{{color}}">
                    <Hds::Badge
                        @icon="activity"
                        @text="Lorem ipsum"
                        @size={{size}}
                        @type={{type}}
                        @color={{color}}
                    />
                    <Hds::Badge
                        @text="Lorem ipsum"
                        @size={{size}}
                        @type={{type}}
                        @color={{color}}
                    />
                    <Hds::Badge
                        @icon="activity"
                        @srOnlyText="Lorem Ipsum"
                        @size={{size}}
                        @type={{type}}
                        @color={{color}}
                    />
                </div>
            {{/each}}
        {{/each}}
    </div>
{{/each}}


{{!-- ###################### --}}


<h2 class="dummy-h2">BadgeCount</h2>

<h3 class="dummy-h3">Content</h3>
<div class="dummy-badge-base-sample">
    <Hds::BadgeCount @text="3" />
    <Hds::BadgeCount @text="99+" />
    <Hds::BadgeCount @text="v1.2.3" />
</div>
<div class="dummy-badge-base-sample">
    <p>This is a paragraph: <Hds::BadgeCount @text="3" /></p>
</div>

<h3 class="dummy-h3">Size</h3>
<div class="dummy-badge-base-sample">
    {{#each @model.SIZES as |size|}}
        <Hds::BadgeCount @text={{size}} @size={{size}} />
    {{/each}}
</div>

<h3 class="dummy-h3">Type</h3>
<div class="dummy-badge-base-sample">
    {{#each @model.TYPES as |type|}}
        <Hds::BadgeCount @text={{type}} @type={{type}} />
    {{/each}}
</div>

<h3 class="dummy-h3">Color:</h3>
{{#each @model.COLORS as |color|}}
    <p class="dummy-h6">{{color}}</p>
    <div class="dummy-badge-color-grid">
        {{#each @model.SIZES as |size|}}
            {{#each @model.TYPES as |type|}}
                <div class="dummy-badge-base-sample dummy-badge-base-sample--type-{{type}} dummy-badge-base-sample--color-{{color}}">
                    <Hds::BadgeCount
                        @text="3"
                        @size={{size}}
                        @type={{type}}
                        @color={{color}}
                    />
                </div>
            {{/each}}
        {{/each}}
    </div>
{{/each}}
