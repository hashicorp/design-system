{{page-title "PaginationBar Component"}}

<h2 class="dummy-h2">PaginationBar</h2>

{{! ADD YOUR CONTENT BELOW }}
{{! Please follow as much as possible what it's already done in other components. }}
{{! You can start by copy&pasting it, if you want, and then adapt it according to your needs. }}
{{! Once done, please remove these comments (they're created by the generator). }}

<section hidden>
  <h3 class="dummy-h3" id="overview"><a href="#overview" class="dummy-link-section">§</a> Overview</h3>

  <p class="dummy-paragraph">
    The PaginationBar component allows users to navigate through a large dataset split into pages. These pages could be
    either links (different URLs) or content rendered within the same page/URL.
  </p>
  <p class="dummy-paragraph">
    It is not meant to be used as a navigation control for regular content or as a stepper, i.e. a guide, tutorial,
    setup or creation flow.
  </p>
</section>

<section hidden>
  <h3 class="dummy-h3" id="component-api"><a href="#component-api" class="dummy-link-section">§</a> Component API</h3>
  <p class="dummy-paragraph">
    The
    <code class="dummy-code">PaginationBar</code>
    component is a wrapper component for the Pagination-related sub-components, each with their own APIs:
  </p>

  <ul class="dummy-list">
    <li>
      a
      <code class="dummy-code">PaginationBar</code>
      “container” component which controls the layout and some of the logic that connects the different sub-components
    </li>
    <li>
      an optional
      <code class="dummy-code">TotalCount</code>
      component which displays the current range of items shown and the total number of items
    </li>
    <li>
      a
      <code class="dummy-code">Pagination</code>
      component which provides navigation controls and optionally displays page numbers
    </li>
    <li>
      an optional
      <code class="dummy-code">PageSize</code>
      component which allows users to change the number of items displayed per page
    </li>
  </ul>

  <h4 class="dummy-h4">PaginationBar</h4>
  <p class="dummy-paragraph" id="component-api-pagination-bar">Here is the API for the main (“container”) component:</p>
  <dl class="dummy-component-props" aria-labelledby="component-api-pagination-bar">
    <dt>totalItems <code>number</code> <strong class="required">required</strong></dt>
    <dd>Pass the total number of items to be paginated. If no value is defined an error will be thrown.</dd>

    <dt>itemsPerPage <code>number</code> <strong class="required">required</strong></dt>
    <dd>
      Pass the maximum number of items to display on each page initially. If no value is defined an error will be
      thrown.
    </dd>

    <dt>currentPage <code>number</code></dt>
    <dd>
      <p>Set a custom initial selected page.</p>
      <p>Default: <span class="default">1</span> (The first page is selected by default.)</p>
    </dd>
  </dl>

  <h4 class="dummy-h4">TotalCount</h4>
  <p class="dummy-paragraph" id="component-api-total-count">
    Here is the API for the
    <code class="dummy-code">TotalCount</code>
    component:
  </p>
  <dl class="dummy-component-props" aria-labelledby="component-api-total-count">
    <dt>showTotalItems <code>boolean</code></dt>
    <dd>
      <p>Hide display of total items in the UI.</p>
      <p>Default: <span class="default">true</span> (Total items are displayed by default.)</p>
    </dd>
  </dl>

  <h4 class="dummy-h4">Pagination</h4>
  <p class="dummy-paragraph" id="component-api-pagination">
    Here is the API for the
    <code class="dummy-code">Pagination</code>
    component:
  </p>
  <dl class="dummy-component-props" aria-labelledby="component-api-pagination">
    <dt>type <code>string</code></dt>
    <dd>
      <p>Sets the type of Pagination.</p>
      <p>Acceptable values:</p>
      <ol>
        <li class="default">compact</li>
        <li>numbered</li>
      </ol>
    </dd>
  </dl>

  <h4 class="dummy-h4">PageSize</h4>
  <p class="dummy-paragraph" id="component-api-page-size">
    Here is the API for the
    <code class="dummy-code">PageSize</code>
    component:
  </p>
  <dl class="dummy-component-props" aria-labelledby="component-api-page-size">
    <dt>sizes <code>array of integers</code> <strong class="required">required</strong></dt>
    <dd>
      <p>Set the page sizes users can select from. If no value is defined an error will be thrown.</p>
      <p>
        Example:
        {{! prettier-ignore-start }}
        {{! template-lint-disable no-unbalanced-curlies }}
        <code>@sizes=\{{array 10 30 50}}</code>
        {{! template-lint-enable no-unbalanced-curlies }}
        {{! prettier-ignore-end }}
      </p>
    </dd>
  </dl>
</section>

<section hidden>
  <h3 class="dummy-h3" id="how-to-use">
    <a href="#how-to-use" class="dummy-link-section">§</a>
    How to use
  </h3>

  <h4 class="dummy-h4">Basic use</h4>
  <p class="dummy-paragraph">Default "compact" pagination with all optional child components</p>
  {{! prettier-ignore-start }}
  {{! template-lint-disable no-unbalanced-curlies }}
  <CodeBlock
    @language="markup"
    @code="
      <Hds::PaginationBar @totalItems=\{{103}} @itemsPerPage=\{{10}} @currentPage=\{{1}} as |P|>
        <P.TotalCount />
        <P.Pagination />
        <P.PageSize @sizes=\{{array 10 30 50}} />
      </Hds::PaginationBar>
    "
  />
  {{! template-lint-enable no-unbalanced-curlies }}
  {{! prettier-ignore-end }}

  <p class="dummy-paragraph">Renders to:</p>
  <Hds::PaginationBar @totalItems={{103}} @itemsPerPage={{10}} @currentPage={{1}} as |P|>
    <P.TotalCount />
    <P.Pagination />
    <P.PageSize @sizes={{array 10 30 50}} />
  </Hds::PaginationBar>

  <h4 class="dummy-h4">“Numbered” pagination</h4>
  {{! prettier-ignore-start }}
  {{! template-lint-disable no-unbalanced-curlies }}
  <CodeBlock
    @language="markup"
    @code='
      <Hds::PaginationBar @totalItems=\{{100}} @itemsPerPage=\{{10}} @currentPage=\{{1}} as |P|>
        <P.TotalCount />
        <P.Pagination @type="numbered" />
        <P.PageSize @sizes=\{{array 10 30 50}} />
      </Hds::PaginationBar>
    '
  />
  {{! template-lint-enable no-unbalanced-curlies }}
  {{! prettier-ignore-end }}

  <p class="dummy-paragraph">Renders to:</p>
  <Hds::PaginationBar @totalItems={{100}} @itemsPerPage={{10}} @currentPage={{1}} as |P|>
    <P.TotalCount />
    <P.Pagination @type="numbered" />
    <P.PageSize @sizes={{array 10 30 50}} />
  </Hds::PaginationBar>
</section>

<section hidden>
  <h3 class="dummy-h3" id="design-guidelines"><a href="#design-guidelines" class="dummy-link-section">§</a>
    Design guidelines</h3>
  {{! UNCOMMENT THIS BLOCK (once the link and/or the image are available) }}
  {{!
  <div class="dummy-design-guidelines">
    <p class="dummy-paragraph">
      <a href="[ADD THE LINK TO THE FIGMA FILE/PAGE HERE!]" target="_blank" rel="noopener noreferrer">Figma UI Kit</a>
    </p>
    <br />
    <img class="dummy-figma-docs" src="/assets/images/pagination-bar-design-usage.png" alt="" role="none" />
  </div>
  }}
</section>

<section hidden>
  <h3 class="dummy-h3" id="accessibility"><a href="#accessibility" class="dummy-link-section">§</a> Accessibility</h3>

  <!-- <Doc::WcagList
    @criteriaList={{array
      "1.3.1"
      "1.3.2"
      "1.4.1"
      "1.4.3"
      "1.4.4"
      "1.4.10"
      "1.4.11"
      "1.4.12"
      "2.1.1"
      "2.1.2"
      "2.4.3"
      "2.4.4"
      "2.4.6"
      "2.4.7"
      "2.5.3"
      "3.2.1"
      "4.1.2"
    }}
  /> -->
</section>

<section data-test-percy>
  <h3 class="dummy-h3" id="showcase">
    <a href="#showcase" class="dummy-link-section">§</a>
    Showcase
  </h3>
  <button id="dummy-toggle-highlight" type="button" {{on "click" this.toggleHighlight}}>
    {{if this.showHighlight "Hide" "Show"}}
    layout highlight
  </button>

  <div class="{{if this.showHighlight 'dummy-pagination-layout-highlight'}}">

    <h4 class="dummy-h4">Base elements</h4>

    <h5 class="dummy-h5">TotalCount</h5>

    <div class="dummy-pagination-base-sample">
      <div>
        <span class="dummy-text-small">default</span>
        <Hds::PaginationBar::TotalCount @itemsRangeStart={{1}} @itemsRangeEnd={{10}} @totalItems={{103}} />
      </div>

      <div>
        <span class="dummy-text-small">
          with
          {{! prettier-ignore-start }}
          {{! template-lint-disable no-unbalanced-curlies }}<code
            class="dummy-code"
          >@showTotalItems=\{{false}}</code>{{! template-lint-enable no-unbalanced-curlies }}
          {{! prettier-ignore-end }}
        </span>
        <Hds::PaginationBar::TotalCount
          @itemsRangeStart={{1}}
          @itemsRangeEnd={{10}}
          @totalItems={{103}}
          @showTotalItems={{false}}
        />
      </div>
    </div>

    <h5 class="dummy-h5">PageSize</h5>
    <Hds::PaginationBar::PageSize @sizes={{array 10 30 50}} />

    <h5 class="dummy-h5">Pagination::NavButton</h5>
    <h6 class="dummy-h6">
      with text label displaying:
    </h6>
    <p class="dummy-paragraph">(text label is shown with Pagination type set to "compact" which doesn't include page
      numbers)</p>

    <div class="dummy-pagination-base-sample">
      {{#let (array "default" "hover" "active" "focus") as |states|}}
        {{#each states as |state|}}
          <div class="dummy-pagination-base-sample-item" mock-state-value={{state}} mock-state-selector="button,a">
            <span class="dummy-text-small">{{state}}</span>
            <div class="dummy-pagination-base-sample-row">
              <Hds::PaginationBar::Pagination::NavButton @direction="previous" @type="compact" />
              <Hds::PaginationBar::Pagination::NavButton @direction="next" @type="compact" />
            </div>
          </div>
        {{/each}}
        <div class="dummy-pagination-base-sample-item">
          <span class="dummy-text-small">disabled</span>
          <div class="dummy-pagination-base-sample-row">
            <Hds::PaginationBar::Pagination::NavButton @direction="previous" @type="compact" @disabled={{true}} />
            <Hds::PaginationBar::Pagination::NavButton @direction="next" @type="compact" @disabled={{true}} />
          </div>
        </div>
      {{/let}}
    </div>

    <h6 class="dummy-h6">
      without text label displaying:
    </h6>
    <p class="dummy-paragraph">(text label is not shown with Pagination type set to "numbered" which includes page
      numbers)</p>
    <div class="dummy-pagination-base-sample">
      {{#let (array "default" "hover" "active" "focus") as |states|}}
        {{#each states as |state|}}
          <div class="dummy-pagination-base-sample-item" mock-state-value={{state}} mock-state-selector="button,a">
            <span class="dummy-text-small">{{state}}</span>
            <div class="dummy-pagination-base-sample-row">
              <Hds::PaginationBar::Pagination::NavButton @direction="previous" @type="numbered" />
              <Hds::PaginationBar::Pagination::NavButton @direction="next" @type="numbered" />
            </div>
          </div>
        {{/each}}
      {{/let}}
      <div class="dummy-pagination-base-sample-item">
        <span class="dummy-text-small">disabled</span>
        <div class="dummy-pagination-base-sample-row">
          <Hds::PaginationBar::Pagination::NavButton @direction="previous" @type="numbered" @disabled={{true}} />
          <Hds::PaginationBar::Pagination::NavButton @direction="next" @type="numbered" @disabled={{true}} />
        </div>
      </div>
    </div>

    <h5 class="dummy-h5">Pagination::PageNumber</h5>
    <p class="dummy-paragraph">Generic vs current page</p>
    <div class="dummy-pagination-base-sample">
      {{#let (array "default" "hover" "active" "focus") as |states|}}
        {{#each states as |state|}}
          <div class="dummy-pagination-base-sample-item" mock-state-value={{state}} mock-state-selector="button,a">
            <span class="dummy-text-small">{{state}}</span>
            <div class="dummy-pagination-base-sample-row">
              <Hds::PaginationBar::Pagination::PageNumber @page="1" />
              <Hds::PaginationBar::Pagination::PageNumber @page="1" @isSelected={{true}} />
            </div>
          </div>
        {{/each}}
      {{/let}}
    </div>

    <h4 class="dummy-h4">Pagination</h4>
    <div class="dummy-paragraph">
      <Hds::PaginationBar::Pagination @totalItems={{100}} @itemsPerPage={{10}} />
    </div>
    <div class="dummy-paragraph">
      <Hds::PaginationBar::Pagination @totalItems={{100}} @itemsPerPage={{10}} @type="numbered" />
    </div>
  </div>

  <h4 class="dummy-h4">PaginationBar with sub-components</h4>

  <h5 class="dummy-h5">Simplest use case: PaginationBar with Pagination</h5>

  <h6 class="dummy-h6">Type="compact" (the default)</h6>
  <Hds::PaginationBar @totalItems={{100}} @itemsPerPage={{10}} @currentPage={{1}} as |P|>
    <P.Pagination />
  </Hds::PaginationBar>

  <h6 class="dummy-h6">Type="numbered"</h6>
  <Hds::PaginationBar @totalItems={{100}} @itemsPerPage={{10}} @currentPage={{1}} as |P|>
    <P.Pagination @type="numbered" />
  </Hds::PaginationBar>

  <h5 class="dummy-h5">PaginationBar with Pagination and TotalCount</h5>
  <h6 class="dummy-h6">Type="compact"</h6>
  <Hds::PaginationBar @totalItems={{100}} @itemsPerPage={{10}} @currentPage={{1}} as |P|>
    <P.TotalCount />
    <P.Pagination />
  </Hds::PaginationBar>

  <h6 class="dummy-h6">Type="numbered"</h6>
  <Hds::PaginationBar @totalItems={{100}} @itemsPerPage={{10}} @currentPage={{1}} as |P|>
    <P.TotalCount />
    <P.Pagination @type="numbered" />
  </Hds::PaginationBar>

  <h6 class="dummy-h6">TotalCount with showTotalItems set to "false"</h6>
  <Hds::PaginationBar @totalItems={{100}} @itemsPerPage={{10}} @currentPage={{1}} as |P|>
    <P.TotalCount @showTotalItems={{false}} />
    <P.Pagination />
  </Hds::PaginationBar>

  <h6 class="dummy-h6">PaginationBar with Pagination, TotalCount, and PageSize</h6>
  <h6 class="dummy-h6">Type="compact"</h6>
  <Hds::PaginationBar @totalItems={{100}} @itemsPerPage={{10}} @currentPage={{1}} as |P|>
    <P.TotalCount />
    <P.Pagination />
    <P.PageSize @sizes={{array 10 30 50}} />
  </Hds::PaginationBar>

  <h6 class="dummy-h6">Type="numbered"</h6>
  <Hds::PaginationBar @totalItems={{100}} @itemsPerPage={{10}} @currentPage={{1}} as |P|>
    <P.TotalCount />
    <P.Pagination @type="numbered" />
    <P.PageSize @sizes={{array 10 30 50}} />
  </Hds::PaginationBar>

  <h5 class="dummy-h5">Numbered Pagination with currentPage set to "3" and items per page = "30"</h5>
  <Hds::PaginationBar @totalItems={{100}} @itemsPerPage={{30}} @currentPage={{3}} as |P|>
    <P.TotalCount />
    <P.Pagination @type="numbered" />
    <P.PageSize @sizes={{array 10 30 50}} />
  </Hds::PaginationBar>
</section>