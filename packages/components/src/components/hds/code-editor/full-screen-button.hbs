<Hds::Button
  class={{this.className}}
  aria-pressed={{@isFullScreen}}
  @isIconOnly={{true}}
  @color="secondary"
  @size="small"
  @icon={{this.icon}}
  @text="Toggle full screen view"
  {{on "click" (fn @onToggleFullScreen)}}
/>