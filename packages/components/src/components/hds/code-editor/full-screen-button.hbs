<Hds::CodeEditor::Button
  class={{this.className}}
  aria-pressed={{@isFullScreen}}
  @isIconOnly={{true}}
  @size="small"
  @icon={{this.icon}}
  @text="Toggle full screen view"
  {{on "click" (fn @onToggleFullScreen (not @isFullScreen))}}
/>