<div class="hds-filter-bar__applied-filters" ...attributes>
  {{#each-in @filters as |key filter|}}
    {{#if filter.data}}
      {{#if (this._isMultiSelectFilter filter)}}
        {{#each (this._multiSelectFilterData filter.data) as |item|}}
          <Hds::Tag
            @text="{{this._filterKeyText key filter}}: {{if item.label item.label item.value}}"
            @ariaLabel={{hds-t "hds.components.filter-bar.applied-filters.tag.aria-label" default="Clear filter"}}
            @onDismiss={{fn this._onFilterDismiss key item.value}}
          />
        {{/each}}
      {{else}}
        <Hds::Tag
          @text={{this._filterText key filter}}
          @ariaLabel={{hds-t "hds.components.filter-bar.applied-filters.tag.aria-label" default="Clear filter"}}
          @onDismiss={{fn this._onFilterDismiss key}}
        />
      {{/if}}
    {{/if}}
  {{/each-in}}
</div>