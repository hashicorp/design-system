{{!
  Copyright (c) HashiCorp, Inc.
  SPDX-License-Identifier: MPL-2.0
}}
{{! template-lint-disable require-presentational-children }}
<li class={{this.classNames}} ...attributes role="presentation">
  {{#if this.isInteractive}}
    <button
      class="hds-stepper-navigation__step__btn"
      id={{this._stepId}}
      tabindex={{unless this.isActive "-1"}}
      type="button"
      role="tab"
      aria-selected={{if this.isActive "true" "false"}}
      aria-controls={{this.coupledPanelId}}
      {{this._setUpStep this.didInsertNode this.willDestroyNode}}
      {{on "click" this.onStepChange}}
      {{on "keyup" this.onKeyUp}}
    >
      <div class="hds-stepper-navigation__step__progress">
        <Hds::Stepper::Step::Indicator
          @text="{{this.stepNumber}}"
          @status={{this.status}}
          @isInteractive={{true}}
          class="hds-stepper-navigation__step__indicator"
        />
      </div>
      <div class="hds-stepper-navigation__step__text">
        <Hds::Text::Body
          class="hds-stepper-navigation__step__title"
          @tag={{this.titleTag}}
          @size="200"
          @weight="semibold"
        >
          <span class="sr-only">{{this.statusSrOnlyText}}</span>
          {{yield to="title"}}
        </Hds::Text::Body>
        <Hds::Text::Body
          class="hds-stepper-navigation__step__description"
          @tag="div"
          @size="100"
          @weight="regular"
          @color="regular"
        >
          {{yield to="description"}}
        </Hds::Text::Body>
      </div>
    </button>
  {{else}}
    <button
      class="hds-stepper-navigation__step__btn hds-stepper-navigation__step__btn-disabled"
      id={{this._stepId}}
      tabindex={{unless this.isActive "-1"}}
      type="button"
      role="tab"
      aria-selected={{if this.isActive "true" "false"}}
      aria-disabled="true"
      aria-controls={{this.coupledPanelId}}
      {{this._setUpStep this.didInsertNode}}
    >
      <div class="hds-stepper-navigation__step__progress">
        <Hds::Stepper::Step::Indicator
          @text="{{this.stepNumber}}"
          @status={{this.status}}
          @isInteractive={{false}}
          class="hds-stepper-navigation__step__indicator"
        />
      </div>
      <div class="hds-stepper-navigation__step__text">
        <Hds::Text::Body
          class="hds-stepper-navigation__step__title"
          @tag={{this.titleTag}}
          @size="200"
          @weight="semibold"
        >
          <span class="sr-only">{{this.statusSrOnlyText}}</span>
          {{yield to="title"}}
        </Hds::Text::Body>
        <Hds::Text::Body
          class="hds-stepper-navigation__step__description"
          @tag="div"
          @size="100"
          @weight="regular"
          @color="regular"
        >
          {{yield to="description"}}
        </Hds::Text::Body>
      </div>
    </button>
  {{/if}}
</li>
{{! template-lint-enable require-presentational-children }}