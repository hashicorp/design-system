<input
  type="range"
  min={{this.min}}
  max={{this.max}}
  step={{this.step}}
  value={{this.value}}
  class={{this.classNames}}
  {{on "input" this.handleInput}}
  {{style this.inlineStyles}}
  ...attributes
/>