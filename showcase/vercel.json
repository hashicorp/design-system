{
  "buildCommand": "yarn workspace @hashicorp/ember-flight-icons build && yarn workspace @hashicorp/design-system-components build && yarn workspace showcase build",
  "devCommand": "yarn workspace showcase ember serve",
  "headers": [
    {
      "source": "/(.*)",
      "headers" : [
        {
          "key" : "X-Robots-Tag",
          "value" : "none"
        }
      ]
    }
  ]
}
