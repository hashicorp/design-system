{{! @glint-nocheck: not typesafe yet }}
{{!
  Copyright (c) HashiCorp, Inc.
  SPDX-License-Identifier: MPL-2.0
}}

{{page-title "Form::SuperSelect Components"}}

<BasicDropdownWormhole />

<Shw::Text::H1>Form::SuperSelect</Shw::Text::H1>

<section data-test-percy>

  <Shw::Text::H2>Single selection</Shw::Text::H2>

  <Shw::Text::H3>Interaction status</Shw::Text::H3>

  <Shw::Grid @columns={{3}} as |SG|>
    <SG.Item @label="Default">
      <Hds::Form::SuperSelect::Single::Base
        @onChange={{this.noop}}
        @options={{@model.OPTIONS}}
        @ariaLabel="Label"
        as |option|
      >
        {{option}}
      </Hds::Form::SuperSelect::Single::Base>
    </SG.Item>
    <SG.Item @label="With placeholder">
      <Hds::Form::SuperSelect::Single::Base
        @onChange={{this.noop}}
        @options={{@model.OPTIONS}}
        @placeholder="Placeholder text"
        as |option|
      >
        {{option}}
      </Hds::Form::SuperSelect::Single::Base>
    </SG.Item>
    <SG.Item @label="Selected">
      <Hds::Form::SuperSelect::Single::Base
        @onChange={{fn (mut @model.SELECTED_OPTION)}}
        @options={{@model.OPTIONS}}
        @selected={{@model.SELECTED_OPTION}}
        as |option|
      >
        {{option}}
      </Hds::Form::SuperSelect::Single::Base>
    </SG.Item>
  </Shw::Grid>

  <Shw::Divider @level="2" />

  <Shw::Text::H3>States</Shw::Text::H3>

  <Shw::Grid @columns={{3}} as |SG|>
    {{#let (array "base" "invalid" "disabled") as |variants|}}
      {{#each @model.STATES as |state|}}
        <SG.Item>
          <Shw::Flex @direction="column" as |SF|>
            {{#each variants as |variant|}}
              {{! template-lint-disable simple-unless }}
              {{#unless (and (eq variant "disabled") (eq state "focus"))}}
                <SF.Item @label="{{capitalize variant}} / {{capitalize state}}">
                  <Hds::Form::SuperSelect::Single::Base
                    @onChange={{fn (mut @model.SELECTED_OPTION)}}
                    @options={{@model.OPTIONS}}
                    @selected={{@model.SELECTED_OPTION}}
                    @disabled={{if (eq variant "disabled") "disabled"}}
                    @isInvalid={{if (eq variant "invalid") true}}
                    class="mock-{{state}}"
                    as |option|
                  >
                    {{option}}
                  </Hds::Form::SuperSelect::Single::Base>
                </SF.Item>
              {{/unless}}
            {{/each}}
          </Shw::Flex>
        </SG.Item>
      {{/each}}
    {{/let}}
  </Shw::Grid>

  <Shw::Divider @level="2" />

  <Shw::Text::H3>List</Shw::Text::H3>

  <Shw::Flex @direction="row" as |SF|>
    <SF.Item {{style flex="1" padding-bottom="11em"}} @label="Default">
      <Hds::Form::SuperSelect::Single::Base
        @onChange={{fn (mut @model.SELECTED_OPTION)}}
        @options={{@model.OPTIONS}}
        @selected={{@model.SELECTED_OPTION}}
        @initiallyOpened={{true}}
        as |option|
      >
        {{option}}
      </Hds::Form::SuperSelect::Single::Base>
    </SF.Item>

    <SF.Item {{style flex="1" padding-bottom="11em"}} @label="Search enabled">
      <Hds::Form::SuperSelect::Single::Base
        @onChange={{fn (mut @model.SELECTED_OPTION)}}
        @options={{@model.OPTIONS}}
        @selected={{@model.SELECTED_OPTION}}
        @searchEnabled={{true}}
        @initiallyOpened={{true}}
        as |option|
      >
        {{option}}
      </Hds::Form::SuperSelect::Single::Base>
    </SF.Item>
  </Shw::Flex>

  <Shw::Text::H4>List position</Shw::Text::H4>

  <Shw::Flex @direction="column" as |SF|>
    <SF.Item>
      <Shw::Flex as |SF|>
        <SF.Item {{style flex="1"}} @label="Auto (default)">
          <Hds::Form::SuperSelect::Single::Base
            @onChange={{fn (mut @model.SELECTED_OPTION)}}
            @options={{@model.OPTIONS}}
            @selected={{@model.SELECTED_OPTION}}
            as |option|
          >
            {{option}}
          </Hds::Form::SuperSelect::Single::Base>
        </SF.Item>
        <SF.Item {{style flex="1"}} @label="Below">
          <Hds::Form::SuperSelect::Single::Base
            @onChange={{fn (mut @model.SELECTED_OPTION)}}
            @options={{@model.OPTIONS}}
            @selected={{@model.SELECTED_OPTION}}
            @verticalPosition="below"
            as |option|
          >
            {{option}}
          </Hds::Form::SuperSelect::Single::Base>
        </SF.Item>
        <SF.Item {{style flex="1"}} @label="Above">
          <Hds::Form::SuperSelect::Single::Base
            @onChange={{fn (mut @model.SELECTED_OPTION)}}
            @options={{@model.OPTIONS}}
            @selected={{@model.SELECTED_OPTION}}
            @verticalPosition="above"
            as |option|
          >
            {{option}}
          </Hds::Form::SuperSelect::Single::Base>
        </SF.Item>
      </Shw::Flex>
    </SF.Item>

    <SF.Item>
      <Shw::Flex as |SF|>
        <SF.Item {{style flex="1"}} @label="Left (+ matchTriggerWidth=False)">
          <Hds::Form::SuperSelect::Single::Base
            @onChange={{fn (mut @model.SELECTED_PLACES_OPTION)}}
            @options={{@model.PLACES_OPTIONS}}
            @selected={{@model.SELECTED_PLACES_OPTION}}
            @matchTriggerWidth={{false}}
            @horizontalPosition="left"
            as |option|
          >
            {{option}}
          </Hds::Form::SuperSelect::Single::Base>
        </SF.Item>
        <SF.Item {{style flex="1"}} @label="Center (+ matchTriggerWidth=False)">
          <Hds::Form::SuperSelect::Single::Base
            @onChange={{fn (mut @model.SELECTED_PLACES_OPTION)}}
            @options={{@model.PLACES_OPTIONS}}
            @selected={{@model.SELECTED_PLACES_OPTION}}
            @matchTriggerWidth={{false}}
            @horizontalPosition="center"
            as |option|
          >
            {{option}}
          </Hds::Form::SuperSelect::Single::Base>
        </SF.Item>
        <SF.Item {{style flex="1"}} @label="Right (+ matchTriggerWidth=False)">
          <Hds::Form::SuperSelect::Single::Base
            @onChange={{fn (mut @model.SELECTED_PLACES_OPTION)}}
            @options={{@model.PLACES_OPTIONS}}
            @selected={{@model.SELECTED_PLACES_OPTION}}
            @matchTriggerWidth={{false}}
            @horizontalPosition="right"
            as |option|
          >
            {{option}}
          </Hds::Form::SuperSelect::Single::Base>
        </SF.Item>
      </Shw::Flex>
    </SF.Item>
  </Shw::Flex>

  <Shw::Text::H4>List width</Shw::Text::H4>

  <Shw::Flex {{style padding-bottom="11em"}} as |SF|>
    <SF.Item {{style min-width="10em"}} @label="matchTriggerWidth = true (default)">
      <Hds::Form::SuperSelect::Single::Base
        @onChange={{fn (mut @model.SELECTED_OPTION)}}
        @options={{@model.OPTIONS}}
        @selected={{@model.SELECTED_OPTION}}
        @initiallyOpened={{true}}
        as |option|
      >
        {{option}}
      </Hds::Form::SuperSelect::Single::Base>
    </SF.Item>
    <SF.Item @label="matchTriggerWidth = false">
      <Hds::Form::SuperSelect::Single::Base
        @onChange={{fn (mut @model.SELECTED_PLACES_OPTION)}}
        @options={{@model.PLACES_OPTIONS}}
        @selected={{@model.SELECTED_PLACES_OPTION}}
        @initiallyOpened={{true}}
        @matchTriggerWidth={{false}}
        as |option|
      >
        {{option}}
      </Hds::Form::SuperSelect::Single::Base>
    </SF.Item>
  </Shw::Flex>

  <Shw::Divider @level="2" />

  <Shw::Text::H3>Before &amp; after options</Shw::Text::H3>

  <Shw::Text::H4>Generic content</Shw::Text::H4>

  <Shw::Flex @direction="row" as |SF|>
    <SF.Item {{style flex="1" padding-bottom="13em"}} @label="Before options content">
      <Hds::Form::SuperSelect::Single::Base
        @beforeOptionsComponent={{component "form/power-select/generic-content"}}
        @searchEnabled={{true}}
        @onChange={{fn (mut @model.SELECTED_PLACES_OPTION)}}
        @options={{@model.PLACES_OPTIONS}}
        @selected={{@model.SELECTED_PLACES_OPTION}}
        @initiallyOpened={{true}}
        as |option|
      >
        {{option}}
      </Hds::Form::SuperSelect::Single::Base>
    </SF.Item>
    <SF.Item {{style flex="1" padding-bottom="16em"}} @label="After options content">
      <Hds::Form::SuperSelect::Single::Base
        @afterOptionsComponent={{component "form/power-select/generic-content"}}
        @searchEnabled={{true}}
        @onChange={{fn (mut @model.SELECTED_PLACES_OPTION)}}
        @options={{@model.PLACES_OPTIONS}}
        @selected={{@model.SELECTED_PLACES_OPTION}}
        @initiallyOpened={{true}}
        as |option|
      >
        {{option}}
      </Hds::Form::SuperSelect::Single::Base>
    </SF.Item>
  </Shw::Flex>

  <Shw::Divider @level="2" />

  <Shw::Text::H3>Options with rich content</Shw::Text::H3>

  <Shw::Flex {{style max-width="50%" padding-bottom="1em"}} as |SF|>
    <SF.Item {{style flex="1" padding-bottom="13em"}} @label="">
      <Hds::Form::SuperSelect::Single::Base
        @selected={{@model.SELECTED_CLUSTER_SIZE_OPTION}}
        @onChange={{fn (mut @model.SELECTED_CLUSTER_SIZE_OPTION)}}
        @options={{@model.CLUSTER_SIZE_OPTIONS}}
        @initiallyOpened={{true}}
        as |option|
      >
        <div class="shw-component-form-super-select-option-rich-content">
          <p class="shw-component-form-super-select-option-rich-header">
            <strong>{{option.size}}</strong>
            <strong>{{option.price}}/hr</strong>
          </p>
          <p>{{option.description}}</p>
        </div>
      </Hds::Form::SuperSelect::Single::Base>
    </SF.Item>
  </Shw::Flex>

  <Shw::Divider @level="2" />

  <Shw::Text::H3>Grouped options</Shw::Text::H3>

  <Shw::Text::Body>
    <mark>
      NOTE: We could provide a standardized option for this and simplify the API if we choose (current styles are for
      demo purposes)
    </mark>
  </Shw::Text::Body>

  <Shw::Flex {{style max-width="50%" padding-bottom="10em"}} @direction="column" as |SF|>
    <SF.Item @label="Custom group component (@groupComponent)">
      <Hds::Form::SuperSelect::Single::Base
        @groupComponent={{component "form/power-select/group-component"}}
        @onChange={{fn (mut @model.SELECTED_GROUPED_OPTION)}}
        @options={{@model.GROUPED_OPTIONS}}
        @selected={{@model.SELECTED_GROUPED_OPTION}}
        @initiallyOpened={{true}}
        as |option|
      >
        {{option}}
      </Hds::Form::SuperSelect::Single::Base>
    </SF.Item>
  </Shw::Flex>

  <Shw::Divider @level="2" />

  <Shw::Text::H3>Field</Shw::Text::H3>

  <Shw::Grid @columns={{3}} as |SG|>
    <SG.Item @label="Only label">
      <Hds::Form::SuperSelect::Single::Field
        @onChange={{fn (mut @model.SELECTED_OPTION)}}
        @options={{@model.OPTIONS}}
        @selected={{@model.SELECTED_OPTION}}
        as |F|
      >
        <F.Label>Lorem ipsum dolor</F.Label>
        {{F.options}}
      </Hds::Form::SuperSelect::Single::Field>
    </SG.Item>
    <SG.Item @label="Label + Helper text">
      <Hds::Form::SuperSelect::Single::Field
        @onChange={{fn (mut @model.SELECTED_OPTION)}}
        @options={{@model.OPTIONS}}
        @selected={{@model.SELECTED_OPTION}}
        as |F|
      >
        <F.Label>This is the label</F.Label>
        <F.HelperText>This is the helper text</F.HelperText>
        {{F.options}}
      </Hds::Form::SuperSelect::Single::Field>
    </SG.Item>
    {{! left empty on purpose }}
    <SG.Item />
    <SG.Item @label="Label + Error">
      <Hds::Form::SuperSelect::Single::Field
        @onChange={{fn (mut @model.SELECTED_OPTION)}}
        @options={{@model.OPTIONS}}
        @selected={{@model.SELECTED_OPTION}}
        @isInvalid={{true}}
        as |F|
      >
        <F.Label>This is the label</F.Label>
        {{F.options}}
        <F.Error>This is the error</F.Error>
      </Hds::Form::SuperSelect::Single::Field>
    </SG.Item>
    <SG.Item @label="Label + Helper text + Error">
      <Hds::Form::SuperSelect::Single::Field
        @onChange={{fn (mut @model.SELECTED_OPTION)}}
        @options={{@model.OPTIONS}}
        @selected={{@model.SELECTED_OPTION}}
        @isInvalid={{true}}
        as |F|
      >
        <F.Label>This is the label</F.Label>
        <F.HelperText>This is the helper text</F.HelperText>
        {{F.options}}
        <F.Error>This is the error</F.Error>
      </Hds::Form::SuperSelect::Single::Field>
    </SG.Item>
    <SG.Item @label="Label + Helper text + Errors">
      <Hds::Form::SuperSelect::Single::Field
        @onChange={{fn (mut @model.SELECTED_OPTION)}}
        @options={{@model.OPTIONS}}
        @selected={{@model.SELECTED_OPTION}}
        @isInvalid={{true}}
        as |F|
      >
        <F.Label>This is the label</F.Label>
        <F.HelperText>This is the helper text</F.HelperText>
        {{F.options}}
        <F.Error as |E|>
          <E.Message>First error message</E.Message>
          <E.Message>Second error message</E.Message>
        </F.Error>
      </Hds::Form::SuperSelect::Single::Field>
    </SG.Item>
  </Shw::Grid>

  <Shw::Text::H3>Required and optional</Shw::Text::H3>

  <Shw::Flex as |SF|>
    <SF.Item @label="With legend + Required">
      <Hds::Form::SuperSelect::Single::Field
        @onChange={{fn (mut @model.SELECTED_OPTION)}}
        @options={{@model.OPTIONS}}
        @selected={{@model.SELECTED_OPTION}}
        @isRequired={{true}}
        as |F|
      >
        <F.Label>Lorem ipsum dolor</F.Label>
        {{F.options}}
      </Hds::Form::SuperSelect::Single::Field>
    </SF.Item>
    <SF.Item @label="With legend + Optional">
      <Hds::Form::SuperSelect::Single::Field
        @onChange={{fn (mut @model.SELECTED_OPTION)}}
        @options={{@model.OPTIONS}}
        @selected={{@model.SELECTED_OPTION}}
        @isOptional={{true}}
        as |F|
      >
        <F.Label>Lorem ipsum dolor</F.Label>
        {{F.options}}
      </Hds::Form::SuperSelect::Single::Field>
    </SF.Item>
  </Shw::Flex>

  <Shw::Divider />

  <Shw::Text::H2>Multiple selection</Shw::Text::H2>

  <Shw::Text::H3>Interaction</Shw::Text::H3>

  <Shw::Flex @direction="row" as |SF|>
    <SF.Item {{style flex="1"}} @label="Default">
      <Hds::Form::SuperSelect::Multiple::Base
        @onChange={{this.noop}}
        @options={{@model.OPTIONS}}
        @ariaLabel="Label"
        as |option|
      >
        {{option}}
      </Hds::Form::SuperSelect::Multiple::Base>
    </SF.Item>

    <SF.Item {{style flex="1"}} @label="Selected">
      <Hds::Form::SuperSelect::Multiple::Base
        @onChange={{fn (mut @model.SELECTED_OPTIONS)}}
        @options={{@model.OPTIONS}}
        @selected={{@model.SELECTED_OPTIONS}}
        as |option|
      >
        {{option}}
      </Hds::Form::SuperSelect::Multiple::Base>
    </SF.Item>
  </Shw::Flex>

  <Shw::Divider @level="2" />

  <Shw::Text::H3>States</Shw::Text::H3>

  <Shw::Grid @columns={{3}} as |SG|>
    {{#let (array "base" "invalid" "disabled") as |variants|}}
      {{#each @model.STATES as |state|}}
        <SG.Item>
          <Shw::Flex @direction="column" as |SF|>
            {{#each variants as |variant|}}
              {{! template-lint-disable simple-unless }}
              {{#unless (and (eq variant "disabled") (eq state "focus"))}}
                <SF.Item @label="{{capitalize variant}} / {{capitalize state}}">
                  <Hds::Form::SuperSelect::Multiple::Base
                    @onChange={{fn (mut @model.SELECTED_PLACES_OPTIONS)}}
                    @options={{@model.PLACES_OPTIONS}}
                    @selected={{@model.SELECTED_PLACES_OPTIONS}}
                    @disabled={{if (eq variant "disabled") "disabled"}}
                    @isInvalid={{if (eq variant "invalid") true}}
                    class="mock-{{state}}"
                    as |option|
                  >
                    {{option}}
                  </Hds::Form::SuperSelect::Multiple::Base>
                </SF.Item>
              {{/unless}}
            {{/each}}
          </Shw::Flex>
        </SG.Item>
      {{/each}}
    {{/let}}
  </Shw::Grid>

  <Shw::Divider @level="2" />

  <Shw::Text::H3>List</Shw::Text::H3>

  <Shw::Flex @direction="row" as |SF|>
    <SF.Item {{style flex="1" padding-bottom="11em"}} @label="Default">
      <Hds::Form::SuperSelect::Multiple::Base
        @onChange={{fn (mut @model.SELECTED_OPTIONS)}}
        @options={{@model.OPTIONS}}
        @selected={{@model.SELECTED_OPTIONS}}
        @initiallyOpened={{true}}
        as |option|
      >
        {{option}}
      </Hds::Form::SuperSelect::Multiple::Base>
    </SF.Item>

    <SF.Item {{style flex="1" padding-bottom="9em"}} @label="Search enabled">
      <Hds::Form::SuperSelect::Multiple::Base
        @onChange={{fn (mut @model.SELECTED_OPTIONS)}}
        @options={{@model.OPTIONS}}
        @selected={{@model.SELECTED_OPTIONS}}
        @searchEnabled={{true}}
        @initiallyOpened={{true}}
        as |option|
      >
        {{option}}
      </Hds::Form::SuperSelect::Multiple::Base>
    </SF.Item>
  </Shw::Flex>

  <Shw::Text::H4>List position</Shw::Text::H4>

  <Shw::Flex @direction="column" as |SF|>
    <SF.Item>
      <Shw::Flex as |SF|>
        <SF.Item {{style flex="1"}} @label="Auto (default)">
          <Hds::Form::SuperSelect::Multiple::Base
            @onChange={{fn (mut @model.SELECTED_OPTIONS)}}
            @options={{@model.OPTIONS}}
            @selected={{@model.SELECTED_OPTIONS}}
            as |option|
          >
            {{option}}
          </Hds::Form::SuperSelect::Multiple::Base>
        </SF.Item>
        <SF.Item {{style flex="1"}} @label="Below">
          <Hds::Form::SuperSelect::Multiple::Base
            @onChange={{fn (mut @model.SELECTED_OPTIONS)}}
            @options={{@model.OPTIONS}}
            @selected={{@model.SELECTED_OPTIONS}}
            @verticalPosition="below"
            as |option|
          >
            {{option}}
          </Hds::Form::SuperSelect::Multiple::Base>
        </SF.Item>
        <SF.Item {{style flex="1"}} @label="Above">
          <Hds::Form::SuperSelect::Multiple::Base
            @onChange={{fn (mut @model.SELECTED_OPTIONS)}}
            @options={{@model.OPTIONS}}
            @selected={{@model.SELECTED_OPTIONS}}
            @verticalPosition="above"
            as |option|
          >
            {{option}}
          </Hds::Form::SuperSelect::Multiple::Base>
        </SF.Item>
      </Shw::Flex>
    </SF.Item>

    <SF.Item>
      <Shw::Flex as |SF|>
        <SF.Item {{style flex="1"}} @label="Left (+ matchTriggerWidth=False)">
          <Hds::Form::SuperSelect::Multiple::Base
            @onChange={{fn (mut @model.SELECTED_PLACES_OPTIONS)}}
            @options={{@model.PLACES_OPTIONS}}
            @selected={{@model.SELECTED_PLACES_OPTIONS}}
            @matchTriggerWidth={{false}}
            @horizontalPosition="left"
            as |option|
          >
            {{option}}
          </Hds::Form::SuperSelect::Multiple::Base>
        </SF.Item>
        <SF.Item {{style flex="1"}} @label="Center (+ matchTriggerWidth=False)">
          <Hds::Form::SuperSelect::Multiple::Base
            @onChange={{fn (mut @model.SELECTED_PLACES_OPTIONS)}}
            @options={{@model.PLACES_OPTIONS}}
            @selected={{@model.SELECTED_PLACES_OPTIONS}}
            @matchTriggerWidth={{false}}
            @horizontalPosition="center"
            as |option|
          >
            {{option}}
          </Hds::Form::SuperSelect::Multiple::Base>
        </SF.Item>
        <SF.Item {{style flex="1"}} @label="Right (+ matchTriggerWidth=False)">
          <Hds::Form::SuperSelect::Multiple::Base
            @onChange={{fn (mut @model.SELECTED_PLACES_OPTIONS)}}
            @options={{@model.PLACES_OPTIONS}}
            @selected={{@model.SELECTED_PLACES_OPTIONS}}
            @matchTriggerWidth={{false}}
            @horizontalPosition="right"
            as |option|
          >
            {{option}}
          </Hds::Form::SuperSelect::Multiple::Base>
        </SF.Item>
      </Shw::Flex>
    </SF.Item>
  </Shw::Flex>

  <Shw::Text::H4>List width</Shw::Text::H4>

  <Shw::Flex {{style padding-bottom="14em"}} as |SF|>
    <SF.Item {{style min-width="10em"}} @label="matchTriggerWidth = true (default)">
      <Hds::Form::SuperSelect::Multiple::Base
        @onChange={{fn (mut @model.SELECTED_OPTIONS)}}
        @options={{@model.OPTIONS}}
        @selected={{@model.SELECTED_OPTIONS}}
        @initiallyOpened={{true}}
        as |option|
      >
        {{option}}
      </Hds::Form::SuperSelect::Multiple::Base>
    </SF.Item>
    <SF.Item @label="matchTriggerWidth = false">
      <Hds::Form::SuperSelect::Multiple::Base
        @onChange={{fn (mut @model.SELECTED_PLACES_OPTIONS)}}
        @options={{@model.PLACES_OPTIONS}}
        @selected={{@model.SELECTED_PLACES_OPTIONS}}
        @initiallyOpened={{true}}
        @matchTriggerWidth={{false}}
        as |option|
      >
        {{option}}
      </Hds::Form::SuperSelect::Multiple::Base>
    </SF.Item>
  </Shw::Flex>

  <Shw::Divider @level="2" />

  <Shw::Text::H3>Before &amp; after options</Shw::Text::H3>

  <Shw::Flex @direction="row" as |SF|>
    <SF.Item {{style flex="1" padding-bottom="15em"}} @label="After options default content">
      <Hds::Form::SuperSelect::Multiple::Base
        @onChange={{fn (mut @model.SELECTED_PLACES_OPTIONS)}}
        @options={{@model.PLACES_OPTIONS}}
        @selected={{@model.SELECTED_PLACES_OPTIONS}}
        @initiallyOpened={{true}}
        as |option|
      >
        {{option}}
      </Hds::Form::SuperSelect::Multiple::Base>
    </SF.Item>
    <SF.Item {{style flex="1" padding-bottom="15em"}} @label="After options text content">
      <Hds::Form::SuperSelect::Multiple::Base
        @afterOptionsContent="After options content"
        @onChange={{fn (mut @model.SELECTED_PLACES_OPTIONS)}}
        @options={{@model.PLACES_OPTIONS}}
        @selected={{@model.SELECTED_PLACES_OPTIONS}}
        @initiallyOpened={{true}}
        as |option|
      >
        {{option}}
      </Hds::Form::SuperSelect::Multiple::Base>
    </SF.Item>
  </Shw::Flex>

  <Shw::Flex @direction="row" as |SF|>
    <SF.Item {{style flex="1" padding-bottom="13em"}} @label="Before options generic content">
      <Hds::Form::SuperSelect::Multiple::Base
        @beforeOptionsComponent={{component "form/power-select/generic-content"}}
        @onChange={{fn (mut @model.SELECTED_PLACES_OPTIONS)}}
        @options={{@model.PLACES_OPTIONS}}
        @selected={{@model.SELECTED_PLACES_OPTIONS}}
        @initiallyOpened={{true}}
        as |option|
      >
        {{option}}
      </Hds::Form::SuperSelect::Multiple::Base>
    </SF.Item>
    <SF.Item {{style flex="1" padding-bottom="15em"}} @label="After options generic content">
      <Hds::Form::SuperSelect::Multiple::Base
        @afterOptionsComponent={{component "form/power-select/generic-content"}}
        @onChange={{fn (mut @model.SELECTED_PLACES_OPTIONS)}}
        @options={{@model.PLACES_OPTIONS}}
        @selected={{@model.SELECTED_PLACES_OPTIONS}}
        @initiallyOpened={{true}}
        as |option|
      >
        {{option}}
      </Hds::Form::SuperSelect::Multiple::Base>
    </SF.Item>
  </Shw::Flex>

  <Shw::Divider @level="2" />

  <Shw::Text::H3>Options with rich content</Shw::Text::H3>

  <Shw::Flex {{style max-width="50%" padding-bottom="14em"}} as |SF|>
    <SF.Item {{style flex="1"}}>
      <Hds::Form::SuperSelect::Multiple::Base
        @searchEnabled={{true}}
        @selected={{@model.SELECTED_CLUSTER_SIZE_OPTIONS}}
        @selectedItemComponent={{component "form/power-select/selected-component"}}
        @onChange={{fn (mut @model.SELECTED_CLUSTER_SIZE_OPTION)}}
        @options={{@model.CLUSTER_SIZE_OPTIONS}}
        @initiallyOpened={{true}}
        as |option|
      >
        <div class="shw-component-form-super-select-option-rich-content">
          <p class="shw-component-form-super-select-option-rich-header">
            <strong>{{option.size}}</strong>
            <strong>{{option.price}}/hr</strong>
          </p>
          <p>{{option.description}}</p>
        </div>
      </Hds::Form::SuperSelect::Multiple::Base>
    </SF.Item>
  </Shw::Flex>

  <Shw::Divider @level="2" />

  <Shw::Text::H3>Field</Shw::Text::H3>

  <Shw::Grid @columns={{3}} as |SG|>
    <SG.Item @label="Only label">
      <Hds::Form::SuperSelect::Multiple::Field
        @onChange={{fn (mut @model.SELECTED_OPTIONS)}}
        @options={{@model.OPTIONS}}
        @selected={{@model.SELECTED_OPTIONS}}
        as |F|
      >
        <F.Label>Lorem ipsum dolor</F.Label>
        {{F.options}}
      </Hds::Form::SuperSelect::Multiple::Field>
    </SG.Item>
    <SG.Item @label="Label + Helper text">
      <Hds::Form::SuperSelect::Multiple::Field
        @onChange={{fn (mut @model.SELECTED_OPTIONS)}}
        @options={{@model.OPTIONS}}
        @selected={{@model.SELECTED_OPTIONS}}
        as |F|
      >
        <F.Label>This is the label</F.Label>
        <F.HelperText>This is the helper text</F.HelperText>
        {{F.options}}
      </Hds::Form::SuperSelect::Multiple::Field>
    </SG.Item>
    {{! left empty on purpose }}
    <SG.Item />
    <SG.Item @label="Label + Error">
      <Hds::Form::SuperSelect::Multiple::Field
        @onChange={{fn (mut @model.SELECTED_OPTIONS)}}
        @options={{@model.OPTIONS}}
        @selected={{@model.SELECTED_OPTIONS}}
        @isInvalid={{true}}
        as |F|
      >
        <F.Label>This is the label</F.Label>
        {{F.options}}
        <F.Error>This is the error</F.Error>
      </Hds::Form::SuperSelect::Multiple::Field>
    </SG.Item>
    <SG.Item @label="Label + Helper text + Error">
      <Hds::Form::SuperSelect::Multiple::Field
        @onChange={{fn (mut @model.SELECTED_OPTIONS)}}
        @options={{@model.OPTIONS}}
        @selected={{@model.SELECTED_OPTIONS}}
        @isInvalid={{true}}
        as |F|
      >
        <F.Label>This is the label</F.Label>
        <F.HelperText>This is the helper text</F.HelperText>
        {{F.options}}
        <F.Error>This is the error</F.Error>
      </Hds::Form::SuperSelect::Multiple::Field>
    </SG.Item>
    <SG.Item @label="Label + Helper text + Errors">
      <Hds::Form::SuperSelect::Multiple::Field
        @onChange={{fn (mut @model.SELECTED_OPTIONS)}}
        @options={{@model.OPTIONS}}
        @selected={{@model.SELECTED_OPTIONS}}
        @isInvalid={{true}}
        as |F|
      >
        <F.Label>This is the label</F.Label>
        <F.HelperText>This is the helper text</F.HelperText>
        {{F.options}}
        <F.Error as |E|>
          <E.Message>First error message</E.Message>
          <E.Message>Second error message</E.Message>
        </F.Error>
      </Hds::Form::SuperSelect::Multiple::Field>
    </SG.Item>
  </Shw::Grid>

  <Shw::Text::H3>Required and optional</Shw::Text::H3>

  <Shw::Flex as |SF|>
    <SF.Item @label="With legend + Required">
      <Hds::Form::SuperSelect::Multiple::Field
        @onChange={{fn (mut @model.SELECTED_OPTIONS)}}
        @options={{@model.OPTIONS}}
        @selected={{@model.SELECTED_OPTIONS}}
        @isRequired={{true}}
        as |F|
      >
        <F.Label>Lorem ipsum dolor</F.Label>
        {{F.options}}
      </Hds::Form::SuperSelect::Multiple::Field>
    </SF.Item>
    <SF.Item @label="With legend + Optional">
      <Hds::Form::SuperSelect::Multiple::Field
        @onChange={{fn (mut @model.SELECTED_OPTIONS)}}
        @options={{@model.OPTIONS}}
        @selected={{@model.SELECTED_OPTIONS}}
        @isOptional={{true}}
        as |F|
      >
        <F.Label>Lorem ipsum dolor</F.Label>
        {{F.options}}
      </Hds::Form::SuperSelect::Multiple::Field>
    </SF.Item>
  </Shw::Flex>
</section>